<h1>New Pass Set</h1>

<%= form_for([@location.user,@location,@pass_set]) do |f| %>
   
 <% if @pass_set.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@pass_set.errors.count, "error") %> prohibited this pass set from being saved:</h2>
      <ul>
      <% @pass_set.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

	<%= f.fields_for :fecha do |builder| %>
		<div class="field">
			<%= builder.label :date %><br />
	    	<%= builder.date_select :date %>
		</div>
	<% end %>

  	<div class="field">
    	<%= f.label :total_released_passes %><br />
    	<%= f.number_field :total_released_passes %>
  	</div>

	<%= f.fields_for :price_point do |builder| %>
		<div class="field">
			<%= builder.label :price %><br />
	    	<%= builder.text_field :price %>
		</div>
	<% end %>

  	<div class="actions">
    	<%= f.submit %>
  	</div>

<% end %>

<%= link_to 'Back', user_location_path(@location.user, @location) %>
