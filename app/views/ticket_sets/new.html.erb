<h1>New Pass Set</h1>

<%= form_for([@bar.user,@bar,@ticket_set]) do |f| %>
   <% if @ticket_set.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ticket_set.errors.count, "error") %> prohibited this pass set from being saved:</h2>

      <ul>
      <% @ticket_set.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  	<div class="field">
    	<%= f.label :date %><br />
    	<%= f.date_select :date %>
  	</div>
  	<div class="field">
    	<%= f.label :total_released_tickets %><br />
    	<%= f.number_field :total_released_tickets %>
  	</div>
  	<div class="field">
    	<%= f.label :price %><br />
    	<%= f.text_field :price %>
  	</div>
  	<div class="field">
		<%= f.label :selling_tickets %><br/>
		<%= f.check_box :selling_tickets %>
  	</div>
  	<div>
  		<%= f.label :description %>
  		<%= f.text_area :description, :rows => "10", :class => "input-xlarge"  %>
  	</div>
  	<div class="actions">
    	<%= f.submit %>
  	</div>
<% end %>

<%= link_to 'Back', user_bar_path(@bar.user,@bar) %>
