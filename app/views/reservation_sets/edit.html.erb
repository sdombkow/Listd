<h1>Editing Reservation Set</h1>

<%= render 'form' %>

<% if params[:location_id] != nil %>
	<%= link_to 'Show', [@location.user, @location, @reservation_set] %> |
	<%= link_to 'Back', [@location.user, @location] %>
<% elsif params[:event_id] != nil %>
	<%= link_to 'Show', [@event.user, @event, @reservation_set] %> |
	<%= link_to 'Back', [@event.user, @event] %>
<% end %>